version: 2.1

jobs:
  build:
    steps:
      - checkout
      - run: python main.python

  test:
     - checkout
     - run: python test.py

  deploy:
     steps:
       -run: echo "PrACTICAL DONE"

workflows:
   build_and_test_deploy:
      jobs:
         - build
         - test:
            requires:
              - build
         - deploy:
            requires:
              - test
            filters:
              branches:
                 only: main                          
              

      
